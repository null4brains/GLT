GLT v1.0

Intended as a simple way to add text to a OpenGL 1.x application. No
textures are used all characters are drawn using glDrawElements as
untextured triangles.

To-do:
add support for later versions of OpenGL
